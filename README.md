# WordPress Custom CSS
Plugin to modify the editor section of the backend of WordPress to make content easier to edit. Mostly it just removes all the extra space the WordPress backend editor takes up.
